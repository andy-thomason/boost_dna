from setuptools import setup

setup(
    name='suffix_array',
    version='0.0.1',
    packages=['suffix_array'],
    # TODO C/C++ extensions and dependencies ...
    install_requires=[
        'pytest',
    ],
)
